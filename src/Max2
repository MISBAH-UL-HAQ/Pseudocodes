Procedure  MaximumOfThreeNumbers

    function maxOfThree(a, b, c):
        if a >= b and a >= c:
            return a
        else if b >= a and b >= c:
            return b
        else:
            return c


End Procedure


//This pseudocode defines a function maxOfThree 
//that takes three parameters (a, b, and c)
//and returns the maximum among them.  
//It checks each number against the other two
//to find the maximum